apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  namespace: infra-full-stack
  labels:
    app: flask-app
spec:
  type: NodePort
  selector:
    app: flask-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30080
      protocol: TCP
      name: http
